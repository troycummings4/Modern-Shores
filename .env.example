# Optional. The site already has a real Amazon Associates tracking ID
# baked in as the default (src/lib/affiliate.ts), so you don't need to set
# anything here to earn commissions.
#
# Only set this if you want to override that tag — e.g. testing a second
# Associates account, or a seasonal/campaign-specific tracking ID.

VITE_AMAZON_ASSOCIATE_TAG=your-alternate-tag-20
